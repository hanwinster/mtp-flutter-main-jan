import 'package:flutter/material.dart';

class OptionIncorrectIcon extends StatelessWidget {
  const OptionIncorrectIcon({super.key});

  @override
  Widget build(BuildContext context) {
    return const Icon(
     Icons.cancel,
      color: Colors.red,
    );
  }
}
