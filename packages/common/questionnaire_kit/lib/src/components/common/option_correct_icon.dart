import 'package:flutter/material.dart';

class OptionCorrectIcon extends StatelessWidget {
  const OptionCorrectIcon({super.key});

  @override
  Widget build(BuildContext context) {
    return const Icon(
     Icons.check_circle,
      color: Colors.green,
    );
  }
}
