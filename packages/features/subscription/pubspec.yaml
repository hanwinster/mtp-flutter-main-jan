name: subscription
description: Subscription feature package.
version: 0.0.1
publish_to: 'none'

environment:
  sdk: '>=3.0.1 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  base:
    path: ../../core/base
  assets:
    path: ../../core/assets
  ui:
    path: ../../core/ui
  domain:
    path: ../../core/domain

  flutter_bloc: ^8.1.3
  bloc_concurrency: ^0.2.5